# Windows-клиент

В этой папке лежит клиентская часть схемы. Серверная часть остаётся в корне репозитория и не смешивается с кодом клиента.

Текущий этап:

- tray-приложение для Windows;
- язык `C++`;
- графический стек `Win32 API`;
- сборка через `CMake`.

Что сейчас закрывается этим подпроектом:

- иконка в трее;
- открытие главного окна по левому клику;
- контекстное меню по правому клику;
- фоновая работа после закрытия окна;
- режим запуска без показа главного окна;
- защита от повторного запуска через именованный mutex;
- сборка на GitHub Actions с публикацией `exe`.

Дальше сюда будут добавляться клиентские модули из схемы:

- модуль авторизации и лицензий;
- модуль обновления баз;
- локальное хранилище сигнатур;
- интеграция со сканирующим модулем.

## Чеклист по текущему ТЗ

Обязательная часть:

- `1)` иконка в трее: сделано;
- `2)` левый клик по иконке показывает главное окно: сделано;
- `3)` правый клик по иконке показывает контекстное меню: сделано;
- `4)` пункт `Открыть` в контекстном меню: сделано;
- `5)` пункт `Выход` в контекстном меню: сделано;
- `6)` повторное добавление иконки после пересоздания панели задач: сделано;
- `7)` запуск без показа главного окна: сделано через флаг `--hidden`;
- `8)` закрытие окна переводит приложение в фоновый режим: сделано;
- `9)` меню главного окна `Файл -> Выход`: сделано;
- `10)` запрет повторного запуска через именованный mutex: сделано;
- `11)` сборка на конвейере через `CMake`: сделано;
- `12)` артефакт сборки в виде `exe`: сделано.

Дополнительные баллы:

- `13)` использовать `WinUI 3.0`: не используется, выбран `Win32 API`;
- `14)` использовать `CMake`: сделано.
